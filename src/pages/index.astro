---


---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>SpotifyMatch</title>
	</head>
	<body>
<div class="input">
<h1>Kies een artiest of vibe!</h1>
<form action="submit">
<div class="inputrow">
  <textarea id="userPrompt" placeholder="Kendrick Lamar... Late night car drive..." rows="1"></textarea>
  <button type="submit">
    <svg id="iconPlay" viewBox="0 0 24 24"><path d="M2 21l21-9L2 3v7l15 2-15 2z"/></svg>
    <svg id="iconPause" viewBox="0 0 24 24" style="display:none"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>
  </button>
</div>
</form>
</div>
<section class="results-section">

<div id="result"></div>

</section>

	</body>

</html>

<style is:global>
  @font-face {
  font-family: "Spotify";
  src: url("/src/pages/fonts/SpotifyMix-Bold.woff2") format("woff2");
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

  body {
    font-family: Spotify;
    display: flex;
    justify-content: center;
    flex-direction: column;
    font-family: "Spotify", monospace;
    align-items: center;
    overflow-x: hidden;
    background-color: #c7dac7;
  }

  .input {
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 1rem;
  margin-bottom: 25vh;
  }

h1 {
  color: rgb(42, 111, 207);
  font-size: clamp(1.5rem, 5.5vw, 3rem);
}

h2 {
  font-size: 2rem;
  color: white;
}



 form {
  display: flex;
  flex-direction: column;
  gap: 1em;
  width: 100%;
  align-items: center;
}

.inputrow {
  display: flex;
  gap: 10px;
  align-items: flex-end;
  width: clamp(300px, 90vw, 600px);
}

textarea {
  flex: 1;
  resize: none;
  font-size: clamp(14px, 2vw, 18px);
  padding: 16px 22px;
  border: 2px solid #837e7e;
  border-radius: 28px;
  outline: none;
  caret-color: #12943f;
}

textarea:focus {
  border-color: #1db954;
}

button {
  width: 54px;
  height: 54px;
  border-radius: 50%;
  background: #1db954;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;

  &:hover {
    transform: scale(1.03);
    transition: ease-in-out;
  }
}

button svg {
  width: 20px;
  height: 20px;
  fill: white;
}

p {
  font-size: 1.2rem;
}

.results-section {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
}

#result {
  display: flex;
  flex-direction: row;
  gap: 1.5rem;
  max-width: 95vw;
  padding-bottom: 2em;
  justify-content: center;


  @media (max-width: 1024px) {
    flex-direction: column;
    align-items: center;
    max-width: 85vw;
  }
} 

.artistContainer {
  min-width: 0;
  padding: 1em;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 10px;

  &:first-of-type {
    background-color: #2e2d2d;
  }
  &:nth-of-type(2) {
    background-color: #12943f;
  }
  &:nth-of-type(3) {
    background-color: rgb(25, 81, 137);
  }
}

.covers {
  display: flex;
  flex-direction: row;
  gap: 1rem;
  width: 100%;
}

.covers img {
  width: 100%;
  height: auto;
  aspect-ratio: 1;
  object-fit: cover;
  border-radius: 8px;
  transition: border-radius 0.2s;

  &:hover {
    border-radius: 30%;
  }
}

.covers a {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-decoration: none;
  transition: transform 0.2s;

  &:hover {
    transform: scale(1.05);
  }
}

.covers p {
  font-size: 0.9rem;
  text-align: center;
  width: 100%;
  color: white;
  margin-top: 4px;
}

.artistContainer:nth-of-type(2) {
  animation-delay: 0.5s;
}

.artistContainer:nth-of-type(3) {
  animation-delay: 1s;
}


</style>

<!-- Hulp van Jad -->
<script>
    // @ts-nocheck
const form = document.querySelector("form");
const textarea = document.querySelector("textarea");
const aiAnswer = document.getElementById("result");
const iconPlay = document.getElementById("iconPlay");
const iconPause = document.getElementById("iconPause");

document.addEventListener("DOMContentLoaded", () => {
  const saved = localStorage.getItem("results");
  if (saved) {
    renderResults(JSON.parse(saved));
  }
});

function setPlaying(playing) {
  iconPlay.style.display = playing ? "none" : "block";
  iconPause.style.display = playing ? "block" : "none";
}

// Hulp van Braham
textarea.addEventListener("keydown", (e) => {
  if (e.key === "Enter" && !e.shiftKey) {
    e.preventDefault();
    form.dispatchEvent(new Event("submit"));
  }
});

function renderResults(results) {
  const resultsContainer = document.getElementById("result");
  resultsContainer.innerHTML = "";

  // Resultaten visueel weergeven, artiest naam, album cover en nummer.
  results.forEach(({ artist, tracks }) => {
    const artistContainer = document.createElement("div");
    artistContainer.classList.add("artistContainer");
artistContainer.innerHTML = `
  <h2>${artist}</h2>
  <div class="covers">
    ${tracks.map(track => `
    <a href="${track.external_urls.spotify}" target="_blank">
    <img src="${track.album.images[1].url}" alt="${track.name}" />
    <p>${track.name}</p>
    </a>
`).join("")}
  </div>
`;
   resultsContainer.appendChild(artistContainer);

artistContainer.style.cursor = "pointer";
artistContainer.addEventListener("click", () => {
  window.location.href = `/artist/${artist}`;
});

// https://developer.mozilla.org/en-US/docs/Web/API/Event/stopPropagation
artistContainer.querySelectorAll(".covers a").forEach(link => {
  link.addEventListener("click", (e) => {
    e.stopPropagation();
  });
});
  resultsContainer.scrollIntoView({ behavior: "smooth", block: "center" });
  });
}

// Error catching wanneer je submit zonder iets in te vullen
form.addEventListener("submit", async (e) => {
  e.preventDefault();
  const userInput = textarea.value;
  if (!userInput) {
    aiAnswer.innerHTML = "Zonder enige input kan ik helaas niks voor je vinden.."
    return;
  }

  setPlaying(true);
  aiAnswer.textContent = "Even zoeken...";

  // WebAI call, hulp van Jad 
  if ('LanguageModel' in self) {
    const session = await LanguageModel.create({
      initialPrompts: [{
        role: "system",
        content: "Je geeft drie muziek artiesten terug gebaseerd op wat de gebruiker als input geeft, alleen de artiesten namen en niks anders. Als de gebruiker een muziekartiest op geeft geef dan drie artiesten die vergelijkbare muziek hebben.  Geef exact drie artiesten terug, elke artiest gescheiden door een komma. Gebruik geen markdown of andere opmaak in je berichten. Geen markdown, geen nummering, geen extra tekst. Geen lege regels. Als je niks uit de user input kan halen en/of de opgegeven input is een naam maar geen spotify artiest zeg dan aan: Sorry hier kan ik geen artiesten uit halen, probeer wat anders!"
      }]
    });
    const result = await session.prompt(userInput);
    console.log(result);
    session.destroy();

const response = await fetch(`/api?artist=${result}&skip_groq=true`);  const data = await response.json();
  if (data.error) {
    aiAnswer.textContent = data.error;
    setPlaying(false);
    return;
  }
  renderResults(data.results, true);
  localStorage.setItem("results", JSON.stringify(data.results));

  } else {

const response = await fetch(`/api?artist=${userInput}`);  
const data = await response.json();

// Error als er niks gevonden kan worden of geen input
if (data.error) {
  aiAnswer.textContent = data.error;
  setPlaying(false);
  return;
}
  renderResults(data.results);
  localStorage.setItem("results", JSON.stringify(data.results));
}

setPlaying(false);


});

    
</script>





